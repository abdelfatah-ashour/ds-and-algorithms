{
  "name": "ds-and-algorithms",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "compile": "tsc",
    "compile:file": "tsc --noEmit",
    "compile-any": "./compile-ts.sh",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Abdelfattah Ashour",
  "license": "ISC",
  "description": "Implementation of a data structure and algorithms library in TypeScript.",
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
